# Bash curl parallel requests

Баш скрипт параллельных запросов с помощью curl нужен для того, чтобы протестировать, как веб сервер обрабатывает различное количество параллельных запросов.

## Примерные необходимые системные требования:

1 гб оперативки </br>
2 ядра процессора </br>
10гб жесткий диск </br>

Технологии, которые необходимы для запуска баш скрипта параллельных запросов и работы с ним:
1. linux с bash
2. curl
3. bc
4. sed

---

## Инструкция по запуску

Эта инструкция поможет вам запустить баш скрипт параллельных запросов с помощью curl:

1. Зайдите в вашу операционную систему linux с bash
2. Перейдите в директирию где вы хотите расположить скрипт, например:
     ```bash
    cd /home/{USER_NAME}/bash-curl-parallel-requests
     ```
3. Если хотите скачать этот скрипт из репозитория через гит
    ```bash
    git clone ...
    ```
   то [Установите git](https://git-scm.com/) (если еще не установлен) </br>
   Либо</br>
   Вы можете скачать этот скрипт из репозитория вручную без гита
4. Проверьте установлен ли у вас curl
    ```bash
    curl --version
    ```
    Если установлен, то пропустите этот шаг
    Если не установлен, то установите
    ```bash
    sudo apt install curl
    ```
    есть и [другие способы установки](https://curl.se/download.html)
5. В скрипте curl-parallel-requests.sh </br>
   - замените значение переменной url на значение вашего урла </br>
   - замените значение переменной totalRequests на значение количества параллельных запросов, которое вы хотите отправить
6. Проверяем что у скрипта curl-parallel-requests.sh есть права на исполнение, если нет, выдаём ему эти права
    ```bash
    chmod +x curl-parallel-requests.sh
    ```
7. Запустите скрипт в терминале командой:
    ```bash
    ./curl-parallel-requests.sh
    ```
8. После получения ответа от всех запросов, в терминале отобразится обработка каждой строки ответа, для составления статистики среднего времени выполнения запросов, примерно так:
   ```
    Обработка строки 1
    Обработка строки 2
    Обработка строки 3
    Обработка строки 4
    Обработка строки 5
    Обработка строки 6
    Обработка строки 7
    Обработка строки 8
    Обработка строки 9
    Обработка строки 10
    Обработка строки 11
    Обработка строки 12
    Обработка строки 13
    Обработка строки 14
    Обработка строки 15
    Обработка строки 16

    16 параллельных запросов завершены успешно из 16 за 1.109975 сек

    За 0 целых секунд, пришло ответов: 15
    За 1 целых секунд, пришло ответов: 1

    Минимальный total_time одного успешного запроса = 0.905459 сек
    Максимальный total_time одного успешного запроса = 1.059349 сек

    Средний total_time одного успешного запроса = 0.916318 сек
    Средний connect_time одного успешного запроса = 0.389779 сек
    Средний pretransfer_time одного успешного запроса = 0.866879 сек
    Средний starttransfer_time одного успешного запроса = 0.916164 сек
    Средний namelookup_time одного успешного запроса = 0.008912 сек
   ```

   При обработке большого количества запросов, например 10.000, если есть ошибки, то будет примерно такой вывод:

   ```
   ...
    Обработка строки 9997
    Обработка строки 9998
    Обработка строки 9999
    Обработка строки 10000
    !!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!! ОШИБКА ОТВЕТА !!!!!!!!!! ОТВЕТ НЕ 200
    requestNumber=9900 HTTP-код:504|504|60.019416|0.000000|0.000180|60.019019|0.000053

    9982 параллельных запросов завершены успешно из 10000 за 117.490235 сек

    За 0 целых секунд, пришло ответов: 9859
    За 1 целых секунд, пришло ответов: 2
    За 2 целых секунд, пришло ответов: 51
    За 3 целых секунд, пришло ответов: 2
    За 4 целых секунд, пришло ответов: 1
    За 7 целых секунд, пришло ответов: 2
    За 11 целых секунд, пришло ответов: 2
    За 35 целых секунд, пришло ответов: 60
    За 36 целых секунд, пришло ответов: 3

    Минимальный total_time одного успешного запроса = 0.002938 сек
    Максимальный total_time одного успешного запроса = 36.048313 сек

    Средний total_time одного успешного запроса = 0.260512 сек
    Средний connect_time одного успешного запроса = 0.006194 сек
    Средний pretransfer_time одного успешного запроса = 0.013292 сек
    Средний starttransfer_time одного успешного запроса = 0.260380 сек
    Средний namelookup_time одного успешного запроса = 0.000069 сек
   ```

9. Так же после выполнения скрипта появится папка ${script_start_time}_logs_and_responces с файлами:
    - responses.log, где лежат все ответы запросов и тайминги ответов в том порядке, в котором мы их получили от сервера. Каждая строка соответствует одному ответу сервера.
    - verboses.log, где лежат логи выполнения всех запросов.

### Важные моменты!

Если производительность веб сервиса (его способность обработки множества паралельных запросов) оказалась ниже, чем вы предполагали, это не всегда связано именно с самим веб сервисом.

Возможно это связано с ограничениями операционной системы на которой запущен веб сервис или с ограничениями операционной системы на которой вы запускаете множество паралельных запросов.

Если вы работаете на Linux, обратите внимание на файлы:
#### /etc/sysctl.conf

и его параметры:</br>
fs.file-max</br>
net.ipv4.ip_local_port_range</br>
net.ipv4.tcp_tw_reuse</br>
net.core.netdev_max_backlog</br>
net.core.somaxconn</br>
net.netfilter.nf_conntrack_max</br>
net.netfilter.nf_conntrack_buckets</br>

Формула для расчета рекомендованного значения nf_conntrack_max = RAM в байтах / 16384.

Формула для расчета рекомендованного значения nf_conntrack_buckets = nf_conntrack_max / 4.

Когда вы скоректируете значение в файле /etc/sysctl.conf, команда sysctl -p поможет применить вам новые значения.

#### /etc/security/limits.conf

и его параметры:</br>
nofile</br>
memlock</br>
nproc</br>
